# Placeholders for every environment variable docker-compose.yml references
# for the admin-panel service. No real secrets - copy to .env and fill in
# real local values.

ADMIN_PANEL_PORT=5174
ADMIN_PANEL_API_URL=http://localhost:8000
ADMIN_PANEL_OAUTH_CLIENT_ID=change_me
